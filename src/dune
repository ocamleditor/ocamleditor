; GENERATED BY OCAMLEDITOR - DO NOT EDIT
; Source: ocamleditor.project.json
; WARNING: task "mkrelease": declares no outputs, so phase Before_clean cannot be represented; emitted as alias @oe-tools-mkrelease. It runs inside dune's build directory, so anything it does to the source tree will not behave as it does under oebuild.
; WARNING: task "mkversion": declares no outputs, so phase Before_clean cannot be represented; emitted as alias @oe-tools-mkversion. It runs inside dune's build directory, so anything it does to the source tree will not behave as it does under oebuild.
; WARNING: task "generate_oebuild_script": declares no outputs, so phase Before_clean cannot be represented; emitted as alias @oe-tools-generate-oebuild-script. It runs inside dune's build directory, so anything it does to the source tree will not behave as it does under oebuild.
; WARNING: task "Install OCamlEditor": declares no outputs, so phase Before_clean cannot be represented; emitted as alias @oe-tools-install-ocamleditor. It runs inside dune's build directory, so anything it does to the source tree will not behave as it does under oebuild.
; WARNING: task "Uninstall OCamlEditor": declares no outputs, so phase Before_clean cannot be represented; emitted as alias @oe-tools-uninstall-ocamleditor. It runs inside dune's build directory, so anything it does to the source tree will not behave as it does under oebuild.
; WARNING: task "distclean": declares no outputs, so phase Before_clean cannot be represented; emitted as alias @oe-tools-distclean. It runs inside dune's build directory, so anything it does to the source tree will not behave as it does under oebuild.
; INFO: task "install": has no phase; reachable only through `dune build @oe-findlib-tools-install'.
; INFO: task "uninstall": has no phase; reachable only through `dune build @oe-findlib-tools-uninstall'.
; INFO: task "reinstall": has no phase; reachable only through `dune build @oe-findlib-tools-reinstall'.
; INFO: task "print": has no phase; reachable only through `dune build @oe-findlib-tools-print'.
; INFO: target "ocamleditor": targets "ocamleditor", "ocamleditor-bytecode" map to the same dune stanza and were merged. In dune the byt/opt variants of a program are (modes ...) on one stanza, not two targets.
; INFO: target "ocamleditor": package "compiler-libs" is not a dune library name; using "compiler-libs.common".
; INFO: target "ocamleditor": package "compiler-libs" is not a dune library name; using "compiler-libs.common".
; INFO: target "ocamleditor": required library "ocamlcommon" resolved to dune library "compiler-libs.common".
; INFO: target "ocamleditor": required library "ocamlcommon" resolved to dune library "compiler-libs.common".
; INFO: target "ocamleditor": depends on "oebuild", which is not a library; a dune stanza cannot depend on it. Generation rules establish the ordering instead.
; INFO: target "ocamleditor": depends on "launcher", which is not a library; a dune stanza cannot depend on it. Generation rules establish the ordering instead.
; INFO: target "ocamleditor": depends on "prepare-build", which is not a library; a dune stanza cannot depend on it. Generation rules establish the ordering instead.
; WARNING: target "ocamleditor": unknown dependency ids 19, 16, 22, 17, 18; ignored.
; INFO: target "ocamleditor-lib": package "compiler-libs" is not a dune library name; using "compiler-libs.common".
; INFO: target "ocamleditor-lib": depends on "oebuild", which is not a library; a dune stanza cannot depend on it. Generation rules establish the ordering instead.
; INFO: target "ocamleditor-lib": depends on "launcher", which is not a library; a dune stanza cannot depend on it. Generation rules establish the ordering instead.
; INFO: target "ocamleditor-lib": depends on "prepare-build", which is not a library; a dune stanza cannot depend on it. Generation rules establish the ordering instead.

; target "prepare-build" / task "err_lexer.ml"
(rule
 (targets err_lexer.ml)
 (deps err_lexer.mll)
 (action (run ocamllex err_lexer.mll)))

; target "prepare-build" / task "err_parser.ml"
(rule
 (targets err_parser.ml err_parser.mli)
 (deps err_parser.mly)
 (action (run ocamlyacc err_parser.mly)))

; target "prepare-build" / task "settings_t.ml"
(rule
 (targets settings_t.ml settings_t.mli)
 (deps settings.atd)
 (action (run atdgen -t settings.atd)))

; target "prepare-build" / task "settings_j.ml"
(rule
 (targets settings_j.ml settings_j.mli)
 (deps settings.atd)
 (action (run atdgen -j settings.atd)))

; target "prepare-build" / task "merlin_t.ml"
(rule
 (targets merlin_t.ml merlin_t.mli)
 (deps merlin.atd)
 (action (run atdgen -t merlin.atd)))

; target "prepare-build" / task "merlin_j.ml"
(rule
 (targets merlin_j.ml merlin_j.mli)
 (deps merlin.atd)
 (action (run atdgen -j merlin.atd)))

; target "prepare-build" / task "find_text_t.ml"
(rule
 (targets find_text_t.ml find_text_t.mli)
 (deps find_text.atd)
 (action (run atdgen -t find_text.atd)))

; target "prepare-build" / task "find_text_j.ml"
(rule
 (targets find_text_j.ml find_text_j.mli)
 (deps find_text.atd)
 (action (run atdgen -j find_text.atd)))

; target "prepare-build" / task "project_t.ml"
(rule
 (targets project_t.ml project_t.mli)
 (deps project.atd)
 (action (run atdgen -t project.atd)))

; target "prepare-build" / task "project_j.ml"
(rule
 (targets project_j.ml project_j.mli)
 (deps project.atd)
 (action (run atdgen -j project.atd)))

; target "prepare-build" / task "oebuild_script.ml"
(rule
 (targets oebuild_script.ml)
 (deps
  generate_oebuild_script.ml
  ../header
  common/utils.ml
  common/file_util.ml
  common/argc.ml
  common/log.ml
  common/shell.ml
  common/ocaml_config.ml
  common/app_config.ml
  common/spawn.ml
  task.ml
  build_script_command.ml
  oebuild/oebuild_util.ml
  oebuild/oebuild_table.ml
  oebuild/oebuild_dag.ml
  oebuild/oebuild_dep.ml
  oebuild/oebuild_dep_dag.ml
  oebuild/oebuild_parallel.ml
  oebuild/oebuild.ml
  build_script_util.ml)
 (action
  (run ocaml -I +unix -I +str str.cma unix.cma generate_oebuild_script.ml)))

; target "tools" / task "mkrelease"
(rule
 (alias oe-tools-mkrelease)
 (action (chdir .. (run ocaml tools/mkrelease.ml))))

; target "tools" / task "mkversion"
(rule
 (alias oe-tools-mkversion)
 (action (chdir ../tools (run ocaml mkversion.ml 2.0.0-ocaml530))))

; target "tools" / task "generate_oebuild_script"
(rule
 (alias oe-tools-generate-oebuild-script)
 (action
  (run ocaml -I +unix -I +str str.cma unix.cma generate_oebuild_script.ml)))

; target "tools" / task "Install OCamlEditor"
(rule
 (alias oe-tools-install-ocamleditor)
 (action (chdir .. (run ocaml tools/install.ml))))

; target "tools" / task "Uninstall OCamlEditor"
(rule
 (alias oe-tools-uninstall-ocamleditor)
 (action (chdir .. (run ocaml tools/uninstall.ml))))

; target "tools" / task "distclean"
(rule
 (alias oe-tools-distclean)
 (action (chdir .. (run ocaml tools/distclean.ml))))

; target "FINDLIB-TOOLS" / task "install"
(rule
 (alias oe-findlib-tools-install)
 (action (chdir .. (run ocaml tools/findlib.ml install))))

; target "FINDLIB-TOOLS" / task "uninstall"
(rule
 (alias oe-findlib-tools-uninstall)
 (action (chdir .. (run ocaml tools/findlib.ml uninstall))))

; target "FINDLIB-TOOLS" / task "reinstall"
(rule
 (alias oe-findlib-tools-reinstall)
 (action (chdir .. (run ocaml tools/findlib.ml reinstall))))

; target "FINDLIB-TOOLS" / task "print"
(rule
 (alias oe-findlib-tools-print)
 (action (chdir .. (run ocaml tools/findlib.ml print))))

(executable
 (name ocamleditor)
 (public_name ocamleditor)
 (modules ocamleditor)
 (libraries
  common
  gmisclib
  icons
  ocamleditor_lib
  oebuildlib
  otherwidgets
  atdgen-runtime
  cairo2
  compiler-libs.common
  curl
  dynlink
  inotify
  lablgtk3
  ocamldiff
  ocp-indent.lib
  odoc-parser
  ppx_deriving.show
  str
  threads.posix
  unix
  xml-light
  yojson)
 (preprocess (pps ppx_deriving.show))
 (flags (:standard -warn-error -A -w -s-y-x-m -g))
 (ocamlopt_flags (:standard -inline 50))
 (link_flags (:standard -g)))

(library
 (name ocamleditor_lib)
 (wrapped false)
 (modules
  (:standard \ generate_oebuild_script ocamleditor ocamleditor_launcher))
 (libraries
  common
  gmisclib
  icons
  oebuildlib
  otherwidgets
  atdgen-runtime
  cairo2
  compiler-libs.common
  curl
  dynlink
  inotify
  lablgtk3
  ocamldiff
  ocp-indent.lib
  odoc-parser
  ppx_deriving.show
  str
  threads.posix
  unix
  xml-light
  yojson)
 (preprocess (pps ppx_deriving.show))
 (flags (:standard -warn-error -A -w -s-y-x-m -g))
 (ocamlopt_flags (:standard -inline 50))
 (library_flags (:standard -g)))

(executable
 (name ocamleditor_launcher)
 (public_name ocamleditorw)
 (modules ocamleditor_launcher)
 (libraries common str unix)
 (flags (:standard -warn-error -A))
 (ocamlopt_flags (:standard -compact))
 (link_flags (:standard -compact))
 (enabled_if (<> %{system} mingw)))
